# Manual mixed-policy rules. Keep Shadowrocket rule syntax and one rule per line.
# These rules are inserted before all curated and remote rules.
DOMAIN,localhost,DIRECT
IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
